(() => {
  const PANEL_ID = 'admin-section-branding';
  const LOGO_BUCKET = 'league-brand-assets';
  function value(id) { return String(document.getElementById(id)?.value || '').trim(); }
  function feedback(message, error = false) { const el = document.getElementById('league-branding-feedback'); if (!el) return; el.hidden = false; el.textContent = message; el.classList.toggle('notice-error', error); }
  function mountPanel() {
    if (document.getElementById(PANEL_ID)) return document.getElementById(PANEL_ID);
    const layout = document.querySelector('.admin-layout'); if (!layout) return null;
    const panel = document.createElement('details'); panel.className = 'panel admin-panel-wide'; panel.id = PANEL_ID;
    panel.innerHTML = `<summary><strong>Liga-Branding</strong></summary><section class="panel admin-panel-accent"><h3>Öffentlicher Auftritt</h3><div class="notice">Diese Einstellungen gelten nur für die aktuell ausgewählte Liga.</div><div class="form-grid section-spacer-top"><div class="field"><label for="league-brand-name">Anzeigename</label><input id="league-brand-name" placeholder="z. B. German Racing League"></div><div class="field"><label for="league-brand-subtitle">Untertitel</label><input id="league-brand-subtitle" placeholder="z. B. Sim Racing Championship"></div><div class="field full"><label for="league-brand-logo-file">Logo hochladen</label><input id="league-brand-logo-file" type="file" accept="image/png,image/jpeg,image/webp,image/svg+xml"><small>PNG, JPG, WebP oder SVG · maximal 2 MB</small></div><div class="field full"><label for="league-brand-logo-url">Logo-URL</label><input id="league-brand-logo-url" type="url" placeholder="Wird beim Upload automatisch eingetragen"></div><div class="field"><label for="league-brand-primary">Primärfarbe</label><input id="league-brand-primary" type="color" value="#35246A"></div><div class="field"><label for="league-brand-secondary">Sekundärfarbe</label><input id="league-brand-secondary" type="color" value="#5A32A3"></div><div class="field"><label for="league-brand-accent">Akzentfarbe</label><input id="league-brand-accent" type="color" value="#2C8FA6"></div></div><div class="card-actions"><button type="button" class="button-secondary" id="upload-league-logo-btn">Logo hochladen</button><button type="button" class="button-primary" id="save-league-branding-btn">Branding speichern</button></div><div id="league-branding-feedback" class="notice" hidden></div></section>`;
    const auth = document.getElementById('admin-section-auth'); if (auth?.nextSibling) layout.insertBefore(panel, auth.nextSibling); else layout.prepend(panel); return panel;
  }
  async function load() { const context = await window.RCCData.getLeagueContext({ forceRefresh: true }); const league = context?.league; if (!league) return; const settings = league.settings && typeof league.settings === 'object' ? league.settings : {}; document.getElementById('league-brand-name').value = settings.brand_name || league.name || ''; document.getElementById('league-brand-subtitle').value = settings.brand_subtitle || ''; document.getElementById('league-brand-logo-url').value = settings.brand_logo_url || league.logo_url || ''; document.getElementById('league-brand-primary').value = settings.primary_color || '#35246A'; document.getElementById('league-brand-secondary').value = settings.secondary_color || '#5A32A3'; document.getElementById('league-brand-accent').value = settings.accent_color || '#2C8FA6'; }
  async function uploadLogo() {
    try {
      await window.requireAdminSession(); const context = await window.RCCData.getLeagueContext({ forceRefresh: true }); if (!['owner','admin'].includes(context?.role)) throw new Error('Nur Owner und Admins dürfen ein Liga-Logo hochladen.');
      const file = document.getElementById('league-brand-logo-file')?.files?.[0]; if (!file) throw new Error('Bitte zuerst eine Logo-Datei auswählen.');
      if (file.size > 2 * 1024 * 1024) throw new Error('Das Logo darf maximal 2 MB groß sein.');
      const allowed = ['image/png','image/jpeg','image/webp','image/svg+xml']; if (!allowed.includes(file.type)) throw new Error('Bitte PNG, JPG, WebP oder SVG verwenden.');
      const ext = file.name.split('.').pop()?.toLowerCase() || 'png'; const path = `${context.league.slug}/logo-${Date.now()}.${ext}`;
      feedback('Logo wird hochgeladen…');
      const { error } = await window.supabaseClient.storage.from(LOGO_BUCKET).upload(path, file, { contentType: file.type, upsert: false }); if (error) throw error;
      const { data } = window.supabaseClient.storage.from(LOGO_BUCKET).getPublicUrl(path); const publicUrl = data?.publicUrl; if (!publicUrl) throw new Error('Öffentliche Logo-URL konnte nicht erzeugt werden.');
      document.getElementById('league-brand-logo-url').value = publicUrl; feedback('Logo wurde hochgeladen. Klicke jetzt auf „Branding speichern“, um es für die Liga zu übernehmen.');
    } catch (error) { console.error(error); feedback(`Logo konnte nicht hochgeladen werden: ${error.message}`, true); }
  }
  async function save() { try { await window.requireAdminSession(); const context = await window.RCCData.getLeagueContext({ forceRefresh: true }); if (!['owner','admin'].includes(context?.role)) throw new Error('Nur Owner und Admins dürfen das Liga-Branding ändern.'); const league = context.league; const settings = { ...(league.settings || {}), brand_name: value('league-brand-name') || league.name, brand_subtitle: value('league-brand-subtitle'), brand_logo_url: value('league-brand-logo-url'), primary_color: value('league-brand-primary'), secondary_color: value('league-brand-secondary'), accent_color: value('league-brand-accent') }; const { error } = await window.supabaseClient.from('leagues').update({ settings }).eq('id', league.id); if (error) throw error; await window.RCCLeagueContext.initialize({ slug: league.slug, forceRefresh: true }); await window.RCCBranding?.apply?.({ forceRefresh: true }); feedback('Liga-Branding wurde gespeichert. Die öffentliche Darstellung wurde aktualisiert.'); } catch (error) { console.error(error); feedback(`Branding konnte nicht gespeichert werden: ${error.message}`, true); } }
  async function init() { const panel = mountPanel(); if (!panel || panel.dataset.initialized === 'true') return; panel.dataset.initialized = 'true'; document.getElementById('upload-league-logo-btn')?.addEventListener('click', uploadLogo); document.getElementById('save-league-branding-btn')?.addEventListener('click', save); try { await load(); } catch (error) { console.error('Liga-Branding konnte nicht geladen werden:', error); } }
  window.RCCAdminBranding = { init, load, save, uploadLogo };
})();
